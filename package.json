{
  "name": "pi-remote-system",
  "version": "0.3.0-rc1",
  "private": true,
  "description": "Self-hosted 24/7 Pi Coding Agent node on an old PC: phone control via Telegram ServerBot, Mac control via HMAC-signed HTTP over Tailscale. No VPS, no port forwarding.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/*.test.ts\"",
    "test:windows": "pwsh -NoProfile -NonInteractive -File installer/tests/Invoke-SmokeTests.ps1",
    "release:windows": "pwsh -NoProfile -NonInteractive -File installer/New-Release.ps1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^22.0.0",
    "typebox": "^1.3.7",
    "typescript": "^5.6.0"
  }
}
